# =====================================================
# FeeMGR Environment Configuration
# =====================================================
# Copy this file to .env.local and fill in your actual values
# After creating .env.local, restart your development server (npm start)
#
# IMPORTANT: Never commit .env.local to version control!
# It should be in .gitignore
# =====================================================

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# REQUIRED: Supabase Configuration
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Your Supabase project credentials
# Get these from: https://app.supabase.com → Your Project → Settings → API
#
# 1. REACT_APP_SUPABASE_URL: Copy the "Project URL" (looks like: https://xxxxx.supabase.co)
# 2. REACT_APP_SUPABASE_ANON_KEY: Copy the "anon" / "public" key (long JWT token starting with eyJ...)
#
# Example:
# REACT_APP_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...

REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL: Google Maps API Configuration
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Used for: Address autocomplete in firm settings and user profiles
# Without this: Address fields will work but won't have autocomplete suggestions
#
# Setup Instructions:
# 1. Go to: https://console.cloud.google.com
# 2. Create or select a project
# 3. Enable APIs: "Places API" and "Maps JavaScript API"
# 4. Create an API key in: APIs & Services → Credentials
# 5. Restrict the key to your domain for security
# 6. Uncomment and add your key below:
#
# REACT_APP_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# OPTIONAL: Sentry Configuration
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Used for: Error tracking and monitoring in production
# Without this: Errors will only be logged to console
#
# Setup Instructions:
# 1. Go to: https://sentry.io
# 2. Create an account and project
# 3. Copy the DSN from project settings
# 4. Uncomment and add your DSN below:
#
# REACT_APP_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Application Version
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Update this when releasing new versions
REACT_APP_VERSION=0.1.0

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Notes
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# - All environment variables MUST start with REACT_APP_ to be accessible in React
# - Changes to .env.local require a server restart (Ctrl+C, then npm start)
# - For production deployment, set these in your hosting platform's environment variables
# - See SETUP.md for complete setup instructions
